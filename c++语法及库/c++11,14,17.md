
# 原始字面量
作用：保留字符串的原始意义，忽略所有的特殊字符，保留原意


**定义方式：==R “xxx(原始字符串)xxx”==其中（）两边的字符串可以省略。

使用原始字面量R “xxx(raw string)xxx”，（）两边的字符串在解析的时候是会被忽略的，**因此一般不用指定**。==如果在（）前后指定了字符串，那么前后的字符串必须相同，否则会出现语法错误。==，**而且必须要用括号把原始字符串括起来

# 类成员的快速初始化

c++98中，在类中只能对静态成员常量初始化（即const static 或static const）

c++11对其进行了扩展，非静态成员也可以在类中初始化==（使用=或{}）==

```cpp
class p
{
	string a="1";✅️
	string b{"1"};✅️
	string l("l");❌️，不能用小括号初始化！！！

}
```

**初始化列表的值会覆盖就地初始化的值**

# final和override
C++中增加了final关键字来限制某个类不能被继承，或者某个虚函数不能被重写，和Java的final关键字的功能是类似的。如果使用final修饰函数，只能修饰虚函数，**并且要把final关键字放到类或者函数的后面。**

```cpp
 void test() final
    {
        cout << "Child class...";
    }


class Child final: public Base
{
public:
    void test()
    {
        cout << "Child class...";
    }
};
```

override关键字**确保在派生类中声明的重写函数与基类的虚函数有相同的签名，同时也明确表明将会重写基类的虚函数，这样就可以保证重写的虚函数的正确性，也提高了代码的可读性，和final一样这个关键字要写到方法的后面。**

==假如要重写的函数函数签名不小心写错了，写了override会提示错误，这样可以增加代码正确性，鲁棒性




# 默认模板参数不需要靠右！！

 



# 数值转换为字符串

```cpp
// 头文件 <string>
string to_string (int val);
string to_string (long val);
string to_string (long long val);
string to_string (unsigned val);
string to_string (unsigned long val);
string to_string (unsigned long long val);
string to_string (float val);
string to_string (double val);
string to_string (long double val);
```
# 字符串转换为数值
c++中数值有多种，不同数值类型有不同的函数
```cpp
// 定义于头文件 <string>
int       stoi( const std::string& str, std::size_t* pos = 0, int base = 10 );
long      stol( const std::string& str, std::size_t* pos = 0, int base = 10 );
long long stoll( const std::string& str, std::size_t* pos = 0, int base = 10 );

unsigned long      stoul( const std::string& str, std::size_t* pos = 0, int base = 10 );
unsigned long long stoull( const std::string& str, std::size_t* pos = 0, int base = 10 );

float       stof( const std::string& str, std::size_t* pos = 0 );
double      stod( const std::string& str, std::size_t* pos = 0 );
long double stold( const std::string& str, std::size_t* pos = 0 );

```
==str==：要转换的字符串
==pos==：传出参数, 记录从哪个字符开始无法继续进行解析, 比如: 123abc, 传出的位置为3
==base==：若 base 为 0 ，则自动检测数值进制：若前缀为 0 ，则为八进制，若前缀为 0x 或 0X，则为十六进制，否则为十进制。

**后两个参数的默认值处理十进制已经足够，所以大部分时间只需传入字符串

**==转换规则==**：
1. 如果字符串中所有字符都是数值类型，整个字符串会被转换为对应的数值，并通过返回值返回
2. 如果字符串的前半部分字符是数值类型，后半部不是，那么前半部分会被转换为对应的数值，并通过返回值返回
3. 如果字符第一个字符不是数值类型转换失败

# 断言和静态断言

## 断言
**==头文件： cassert或assert.h


断言：一个返回bool类型的**宏**，特点是只有返回为真时才会继续运行，否则强制退出并提示是哪个断言错误了，常用于调试

```cpp
char* createArray(int size)
{
    // 通过断言判断数组大小是否大于0
    assert(size > 0);	// 必须大于0, 否则程序中断
    char* array = new char[size];
    return array;
}
```

但是assert只能用于运行时断言，只有程序运行之后我们才能知道一些条件对不对
## 静态断言

**==在编译阶段就能检查的断言==**
**而且可以自定义错误信息**
头文件：不需要
作用：用于一些编译时就必须知道是否正确的东西，比如64还是32位平台
```cpp
static_assert(sizeof(long) == 4, "错误, 不是32位平台...");
```
参数1：断言表达式，这个表达式通常需要返回一个 bool值
参数2：警告信息，它通常就是一段字符串，在违反断言（表达式为false）时提示该信息



**==由于是编译阶段的检查，所以表达式中不能出现变量，也就是说必须是常量表达式**==



# noexcept
修饰一个函数并表示它不会抛出异常
```cpp
double divisionMethod(int a, int b) noexcept
{
    if (b == 0)
    {
        cout << "division by zero!!!" << endl;
        return -1;
    }
    return a / b;
}
```
若还是抛出异常，则直接中断程序运行，不会触发栈展开，减少开销


从语法上讲，noexcept 修饰符有两种形式：

1. 简单地在函数声明后加上 noexcept 关键字

2. 可以接受一个常量表达式作为参数，如下所示∶
```cpp
double divisionMethod(int a, int b) noexcept(常量表达式);
常量表达式的结果会被转换成一个bool类型的值：
```

值为 true，表示函数不会抛出异常
值为 false，表示有可能抛出异常这里
不带常量表达式的noexcept相当于声明了noexcept（true），即不会抛出异常。

# auto

**使用auto声明的变量必须要进行初始化，以让编译器推导出它的实际类型，在编译时将auto占位符替换为真正的类型**

## 注意
**==auto 会忽略引用和顶层const==**

何为顶层const？：限制变量本身的const

何为底层const？：限制数据的const

比如对指针来说，const int * *p＝10这个const是底层的，因为它限制的10这个变量，如果auto忽略了const会导致修改新变量时把10给改了，所以编译器必须保留底层const

```cpp
const int p =10;
auto x=p;//x会被推到为const int *类型
```

而int * const p＝10，则是顶层const，因为它限制的p这个变量，但当修改新变量的时候改的是新变量，而非原变量，所以不破坏规则，故而编译器会忽略顶层const

```cpp
int * const p=10;
auto x=p;//这里x被推导为int *
```

为什么？：
**==auto默认按值推导，底层是模板类型推导，默认是把变量值复制一份到auto指定的新变量==**

1. 顶层const只确保变量不能被改变，但auto是复制一份给新变量，新变量不会受到原变量const的影响
2. 但底层const直接锁死指针指向的数据，这意味着复制过去的新变量也必须遵守不能改数据的规则，所以底层const将被保留

### 如何避免？

**==当涉及const 和 & 时，手动给auto声明上const 和 &==**
```cpp
const auto ..
auto const...
auto &
const auto & ...
....
```

# decltype

## 作用
自动类型推导（直接获取类型）
## 语法
```cpp
decltype(表达式)  变量名;
```
## 注意

decltype不复制副本，而是直接获取类型，所以不会忽略&和const.

1. 当括号里的是变量名时：**直接获取该变量类型**。
2. 当括号里的是表达式时：**获取的是表达式的返回类型**。
例如：
```cpp
vector<int> v; 
auto x = v[0]; 
decltype(v[0]) y = v[0];

这里x是int类型，y是int&类型，因为vector访问元素返回的是&
```

```cpp
int a = 10;

decltype((a));
这里得到的是int &类型
（a）是普通变量表达式，表示对一个元素的访问，返回的是元素的左值
```

# decltype（auto）
## 作用
类型推导，适用于泛型编程的函数返回值等，**可以避免用auto会偷偷复制大型对象

## 语法

```cpp
int a = 10;

int& ref = a;


auto x = ref;

decltype(auto) y = ref;
```

**==等价于==**
```cpp
decltype(ref) y =ref;
```
所以y的类型是int&

## 作用举例

```cpp
vector<string> data;

auto get() { return data[0]; }//会复制一份

decltype(auto) get() { return data[0]; }//可以返回引用
```

# 范围for循环

**==只可以进行顺序遍历==**

语法
```cpp
for (auto value : t)
    {
        cout << value << " ";
    }
这样是拷贝，也可以加&变引用
```

一些细节：
1. 如果容器本身就是只读性质的（比如set）那么不论auto有没有加const获取的值都是只读的
2. 范围for只获取一次容器，获取到容器后，不论循环多少次都不会再重新获取容器，也就是说**==冒号后面的表达式只执行一次==**

**结论：对应基于范围的for循环来说，冒号后边的表达式只会被执行一次。在得到遍历对象之后会先确定好迭代的范围，基于这个范围直接进行遍历。如果是普通的for循环，在每次迭代的时候都需要判断是否已经到了结束边界。**


# nullptr

**使用nullptr替代NULL和0表示空指针能使代码更具有健壮性**

原因：假如有两个重载函数：void fun（char* a）和void fun（int p)
调用fun（NULL）
**会自动匹配整数参数的函数，因为NULL和0完全等价，但有时候实际上我们希望调用的是匹配参数为指针的重载函数

==而使用nullptr可以自动匹配参数为指针的重载函数==


# 右值引用和移动语义
## 一些概念
**==移动语义：直接拿即将销毁的对象的数据，不复制**==
**==左值：有名字，有身份，可以取地址的东西==**
**==右值：临时产生，存在的东西==**
**==左值引用必须绑定左值==**
```cpp
int& ref = 10;//错误，不能给一个即将消失的东西长期引用
```

**==右值引用(&&)==**

```cpp
int&& ref = 10;//专门接受右值
```
**注意：这里ref是左值！！！，因为它有名字，本身是左值，类型是右值引用不代表表达式或变量就是右值。

## 移动构造
移动构造函数：
```cpp
String(String&& other) //传入右值
{ 
data = other.data; //把右值的数据移动

other.data = nullptr;//把右值数据指针置空

 }
```
调用移动构造：
```cpp
String a("hello"); 
String b=std::move(a);
```
**std::move不移动东西，它只是把变量标记为右值，告诉编译器，允许移动**

**被标记为右值的变量资源移动完并不会立刻被销毁，他们仍有原来的生命周期，而且移动后指针置空，重新指向后那么还能继续使用。

==主要用于移动对象管理的资源所有权：
资源包括：
- 堆内存（指针指向new出来的东西）
- 文件句柄
- socket连接
- 锁
- GPU资源
- 操作系统资源

==对于一些普通内置类型：int，double,char,这些本身就在栈上的，移动和复制几乎可以等价，因为没有什么需要额外偷走的资源。**堆内存复制慢是因为深拷贝还需要额外申请空间啥的。**

**==所以没有资源的数据，移动通常就是一次普通复制==**

## 移动赋值
用于对象已经存在
```cpp
String& operator=(String&& other) 
{ 
delete[] data; //多一步清理当前数据
data = other.data; 
other.data = nullptr; 
return *this; 
}
```

## 类资源管理五法则

==若手动定义了析构、拷贝构造、拷贝赋值中的任意一个，应明确定义全部五个特殊成员函数。==,**因为说明你正在管理资源，那这五个函数是配套的，缺一不可，比如，你写析构函数意味你要释放堆内存等，那你的其他四个函数都要支持“深拷贝”，所以需要重写
1. 析构函数
2. 复制构造
3. 复制赋值
4. 移动构造
5. 移动赋值

**零法则**：现代c++推荐资源管理用智能指针等自带库来做，尽量不要自己管理，不要重写这五个函数的任何一个

# 完美转发
## 问题

用于解决，**函数传参会丢右值信息**。
```cpp
void wrapper(string&& s) 
{ 
process(s); 
}
```
当有一个中间函数，虽然s接收的是右值，但s本身是左值，所以调用s的时候，调用的会是process的&版本而非&&版本。

## forward
**作用，查看当初传入函数赋值给s的变量是左值还是右值，并恢复。**
```cpp
void wrapper(string&& s) { 

process(std::move(s)); 

}
```
这样写可以，但是这样就强制移动了，也不灵活，forward多用于模板

**解决：
```cpp
void wrapper(string&& s) { 

process(std::forward<string>(s)); 

}
```
forward的作用：==**保留参数进入函数之前的左值/右值属性**。==保持原样转发

# 万能引用（转发引用）

仅在模板中成立
```cpp
template<typename T> 
void func(T&& x) 
{ 
}
```

**==这里&&不一定是右值==**，因为T会变化，c++有一套引用折叠规则：

| 情况       | 结果    |
| -------- | ----- |
| `T& &`   | `T&`  |
| `T& &&`  | `T&`  |
| `T&& &`  | `T&`  |
| `T&& &&` | `T&&` |
	**==只有实际传入类型和模板声明类型同时满足&&最终才会是T&&，否则，都被折叠为T&

# 智能指针
头文件：
```cpp
#include <memory>
```

**用对象管理动态资源的生命周期，让资源自动释放。（不需要主动释放）**

## RAII（Resource Acquisition Is Initialization）

**==对象获取即初始化**==

## unique_ptr

**==唯一拥有==**
大小，通常八字节，若带状态删除器，可能16字节
### 语法
```cpp
unique_ptr<int> p;//声明
auto p = make_unique<int>(10);//创建变量
auto p = make_unique<int[]>(10);//创建数组
```

### 独占性
unique_ptr不能复制
```cpp
auto p1 = make_unique<int>(10); //但是make_unique不能指定删除器
auto p2 = p1;//错误！！！！×
```

### move
但是可以移动（move）所有权
```cpp
auto p1 = make_unique<int>(10); 
auto p2 = move(p1);

```
移动完后p1变为nullptr，而p2拥有对象

### 主动释放
用**reset**可以主动释放或替换智能指针资源
```cpp
auto p = make_unique<int>(10); 
p.reset();//p==nullptr

//也可以
p.reset(new int(20));

//但更推荐
p = make_unique<int>(20);//直接创建新的并管理
```

### 获取原始指针
#### get

作用：获取资源地址，不转移所有权，不影响生命周期
有时候需要用c风格指针调用旧接口
```cpp
auto p = make_unique<int>(10); 
int* raw = p.get();
```

**==注意不要delete raw，因为资源仍然属于智能指针，否则会二次释放==**

#### release
注意：放弃所有权，并返回原始指针
```cpp
int* raw = p.release();//p==nullptr
```

需要手动delete raw

### 删除器
**==智能指针默认的释放资源方式是delete，但有些资源释放并不是靠delete，所以需要自己定义一个删除器==**

`unique_ptr` 的模板：

```cpp
template<class T, class Deleter = default_delete<T>>
class unique_ptr;
```

第二个模板参数就是删除器类型。

声明
```cpp
std::unique_ptr<T, 删除器类型> ptr;
```
#### 使用函数作为删除器
例：
```cpp
#include <iostream> 
#include <memory> 
void deleteFunc(int* p) {
 std::cout << "释放资源\n"; 
 delete p; 
 } 
 int main() { 
 std::unique_ptr<int, void(*)(int*)> ptr( new int(10), deleteFunc ); //删除器类型这里传入函数指针
 }
```
#### 使用 Lambda 作为删除器
例：
```cpp
auto deleter = [](int* p) 
{ std::cout<<"释放\n"; 
delete p; 
}; 
std::unique_ptr<int, decltype(deleter)> ptr( new int(100), deleter );
```











## shared_ptr

### 语法
```cpp
std::shared_ptr<int> p(new int(10));//创建，构造函数
auto p = std::make_shared<int>(10);//创建，推荐这种
```

### 共享性
shared_ptr 通过 **==引用计数==** 实现一个资源被多个对象共享，**==引用计数为0时释放资源==**
**引用计数**：一个资源被几个共享指针所拥有
### 常用函数

#### use_count()

查看引用数量：

```cpp
auto p1 = make_shared<int>(10);

cout << p1.use_count();
```

输出：

```cpp
1
```

---

复制：

```cpp
auto p2 = p1;

cout << p1.use_count();
```

输出：

```cpp
2
```


#### reset()

释放当前 shared_ptr：

```cpp
auto p = make_shared<int>(10);

p.reset();
```

之后：

```cpp
p == nullptr
```

#### get()

和 unique_ptr 一样：

```cpp
int* raw = p.get();
```

只是获取地址：

- 不增加引用计数
- 不转移所有权

#### move

同样支持移动：

```cpp
auto p1 = make_shared<int>(10);

auto p2 = move(p1);
```

结果：

```cpp
p1:
nullptr

p2:
 |
10
```

注意：

这里不是复制。

引用计数不会增加。

### 坑！！（循环引用）

例如：

```cpp
class A;

class B
{
public:
    shared_ptr<A> a;
};


class A
{
public:
    shared_ptr<B> b;
};
```

现在：

```cpp
auto a = make_shared<A>();
auto b = make_shared<B>();

a->b = b;
b->a = a;
```

形成：

```
a
|
v
b
^
|
a
```

引用计数：

```
A count = 2
B count = 2
```

离开作用域：

外部指针销毁：

```
A count = 1
B count = 1
```

永远不会 0。

内存泄漏。


## weak_ptr
### 核心

**==weak_ptr是一个不拥有资源的指针，它是观察用来观察shared_ptr管理对象的智能指针

**重要作用：解决shared_ptr的循环引用问题


weak_ptr**不增加引用计数**，所以，当可能出现循环引用问题时，**把其中一个shared_ptr换成weak_ptr就可以避免对象销毁后引用计数仍大于0**


**weak_ptr不保证对象还存在，所以虽然指向对象，weak_ptr也不能直接访问，==所以使用之前，必须把weak_ptr转换为shared_ptr使用

**weak_ptr==不能直接创建对象，只能依赖于观察sp==**

### 基本声明语法
声明一个weak_ptr
```cpp
std::weak_ptr<int> wp;
```
此时wp指向空，没有观察某个shared_ptr

**令wp观察sp,注意wp不能观察一个临时的sp对象**
```cpp
std::shared_ptr<int> sp = std::make_shared<int>(100);
或
std::weak_ptr<int> wp = sp;（sp是一个共享指针）
```

### 访问lock()

**如果weak_ptr想要访问对象，必须使用lock方法，返回一个shared_ptr，来访问资源，当然如果原对象已经销毁，那么返回的shared_ptr会是==空指针==

```cpp
auto temp = wp.lock();
temp是共享指针
所以temp存在期间可以保证对象不会被销毁（temp不是空指针的情况下）
```

**但是，对象可能已经销毁，所以得加上空指针判断

```cpp
if (auto temp = wp.lock())
{
    std::cout << *temp << std::endl;
}
```

### 判断对象是否销毁

```cpp
bool flag = wp.expired();
返回0表示对象还存在，返回1表示已经销毁
```

### 停止观察对象

```cpp
wp.reset();
```

### use_count

```cpp
int num =wp.use_count;
```
返回当前有多少个sp共享这个对象，引用计数

### swap
**交换两个wp的观察对象**
```cpp
wp1.swap(wp2);
```



## sp的控制块和wp的底层关系

**sp的构成可以理解为，对象+控制块（存放着管理对象生命周期的东西）**，==同一个对象，它的sp用的控制块也必须一样==

==实际上之前我们说的引用计数是**强引用计数**，也就是有几个shared_ptr管理着对象，实际上控制块里还有弱引用计数，记录的是有几个wp观察着对象==

**关键：对象和控制块不是同时销毁的！！！！**
1. 对象销毁于**强引用计数**为0时
2. 控制块销毁于**强，弱引用计数**都为0时

==所以，wp在对象销毁后仍能进行expired（）等操作是因为sp的控制块还没销毁！！！==

## 为什么make_shared比new高效

1. **make_shared**可以一次性动态分配对象和控制块给sp，所以通常只需一次动态内存分配
2. 而**new**一般要两次动态内存分配，第一次分配对象，第二次才分配控制块

**注意：如果要自定义删除器，那么make_shared无法胜任**

## 复制一个sp管理同一个对象的正确做法


**==错误做法==**
```cpp
int* p = new int(100);

std::shared_ptr<int> sp1(p);
std::shared_ptr<int> sp2(p);
```
原因：**这样子sp1和sp2的控制块无法共享，两个都以为只有自己拥有对象，所以会出现重释放问题**

**==正确做法


```cpp

auto sp1 = std::make_shared<int>(100);

auto sp2 = sp1;

或

std::shared_ptr<int> sp2(sp1);

```
这种复制才能把控制块也给过去，一同指向



# Lambda


**可以理解为没有名字的，有operator（）的类对象，即伪函数**
## 基本语法
```cpp
[capture](parameters) -> return_type
{
    // 函数体
}
```


[]          → 捕获列表
(int a,b)   → 参数列表
-> int      → 返回类型
{...}       → 函数体

## 调用

```cpp
int main()
{
    []()
    {
        std::cout << "Hello" << std::endl;
    }();//这里这个();才是调用！！！！！
}
```

## 保存Lambda表达式

常用的：
```cpp
auto func = []()//注意要用auto，因为Lambda表达式是闭包类型
{
    std::cout << "Hello" << std::endl;
};

func();
func();
```
**这里func是一个类对象**

特殊的：
对于**没有捕获任何变量的lambda表达式，还可以转换成一个普通的函数指针**：
```cpp
using func_ptr = int(*)(int);
// 没有捕获任何外部变量的匿名函数
func_ptr f = [](int a)
{
    return a;  
};
// 函数调用
f(1314);
```



## 返回值

```cpp
auto add = [](int a, int b)
{
    return a + b;
};//Lambda有自动类型推导，比如这里自动推导出返回类型为int
```

但是你也可以自己指定返回值
```cpp
auto add = [](int a, int b) -> double
{
    return a + b;
};
```

## 捕获

**==如果Lambda表达式中要用到的，参数列表外的外部局部变量==**

### 按值捕获[=]

把变量按照值捕获（类似值传递），捕获进来。

```cpp
int x = 10;

auto func = [=]()
{
    std::cout << x << std::endl;
};
```

### 按引用捕获[&]

把变量按引用捕获（类似引用传递），捕获进来

```cpp
int x = 10;

auto func = [&]()
{
    x = 20;
};

func();

std::cout << x << std::endl;
```

**==注意，[=]和[&]并不是把外部所有变量都捕获为Lambda成员，只有Lambda内部用到了，且符合捕获规则的变量才会被捕获

### 捕获列表速查

| 写法        | 含义             |
| --------- | -------------- |
| `[]`      | 不捕获外部变量        |
| `[=]`     | 外部变量按值捕获       |
| `[&]`     | 外部变量按引用捕获      |
| `[x]`     | 按值捕获 `x`       |
| `[&x]`    | 按引用捕获 `x`      |
| `[x, &y]` | `x` 按值，`y` 按引用 |
## mutable

Lambda表达式内部默认不能更改Lambda对象内部的成员对象(**operator()const**)，所以也就是说捕获来的变量不可在Lambda表达式内更改

**核心作用：使Lambda内部捕获的变量可更改

```cpp
auto f = [x]() mutable
{
    x++;
};
```


# constexpr

## const
:可以指定一个变量是**只读的**，==但这个常量不一定是常量==
比如
```cpp
void func(const int a)
```
这里虽然定义了a为const，但a只是在函数体内不可更改，但传入不同的值a会变化，所以只能说a是只读的
```cpp
const int&b=a1;
```
b=a2是不允许的，但a1的值可以变，然后b的值也会跟着变，所以这里b不能说是常量
```cpp
const int a=24;
```
这里a才是常量

## constexpr

**用来修饰一个变量是常量**,==注意与const不同==,**在编译期时便求值**

**规则：const和constexpr要区分应用场景（只读时用const，常量时用constexpr）

不过，在定义常量时，const和constexpr是完全等价的

# constexpr用作修饰函数

1. 函数必须要有返回值，并且return 返回的表达式必须是常量表达式。
2. 函数在使用之前，必须有对应的定义语句。定义语句必须在调用语句上面，因为常量表达式函数是在编译期就要确定怎么算
3. 整个函数的函数体中，不能出现非常量表达式之外的语句（using 指令、typedef 语句以及 static_assert 断言、return语句除外）。（例如，for循环，while循环等是非法的）
```cpp
constexpr int func()
    {
        constexpr int var = 100;
        return 5 * var;
    }
```


### 修饰模板函数

关键：因为模版函数实例化时有不确定性，**所以如果实例化出来的函数符合constexpr条件则应用，若不符合则自动忽略constexpr关键字**

### 常量构造函数

如果想要直接构造一个常量对象，可以用constexpr修饰构造函数

```cpp
struct Person {
    constexpr Person(const char* p, int age) :name(p), age(age)
    {
    }
    const char* name;
    int age;
};
```

**==常量构造函数的函数体必须为空，且必须用初始化列表给各个成员赋值



# 委托构造函数

解决什么问题？：同一个类中不同的构造函数可能会有重复的代码
**委托构造函数允许构造函数使用同一个类中的另一个构造函数进行初始化

例子：
```cpp
Test() {};
    Test(int max)
    {
        this->m_max = max > 0 ? max : 100;
    }

    Test(int max, int min):Test(max)
    {
        this->m_min = min > 0 && min < max ? min : 1;
    }

    Test(int max, int min, int mid):Test(max, min)
    {
        this->m_middle = mid < max && mid > min ? mid : 50;
    }



```

**==注意事项==**
1. 调用关系不要形成闭环，否则会运行期抛异常
2. 调用构造函数要在初始化列表构造，在函数体内部调用会提示参数重定义
3. 调用委托构造函数初始化某个成员之后，就不能再在初始化列表初始化那个成员了


# 继承构造函数

有时候子类可以直接用父类的构造函数，而不需要再自己写(**如果父类构造函数和子类构造函数一致的话**)


使用语句：**using 父类名：：父类构造函数名** 即可（实际上父类构造函数名和父类名相同）

```cpp
#include <iostream>
#include <string>
using namespace std;

class Base
{
public:
    Base(int i) :m_i(i) {}
    Base(int i, double j) :m_i(i), m_j(j) {}
    Base(int i, double j, string k) :m_i(i), m_j(j), m_k(k) {}

    int m_i;
    double m_j;
    string m_k;
};

class Child : public Base
{
public:
    using Base::Base;
};

int main()
{
    Child c1(520, 13.14);
    cout << "int: " << c1.m_i << ", double: " << c1.m_j << endl;
    Child c2(520, 13.14, "i love you");
    cout << "int: " << c2.m_i << ", double: " 
         << c2.m_j << ", string: " << c2.m_k << endl;
    return 0;
}

```

==**如果子类中的函数隐藏了父类中的同名函数，那么也可以用 using 父类名：：函数名，直接在子类中调用父类函数（也就是调用时不需要特别指出作用域）**==



# using 的使用

[见](obsidian://open?vault=Obsidian%20Vault&file=c%2B%2B%E8%AF%AD%E6%B3%95%E5%8F%8A%E5%BA%93%2F%E8%B5%B7%E5%88%AB%E5%90%8D%EF%BC%88using%EF%BC%89)




# 列表初始化

```cpp
int a={520};
int a{520};
```
两种都是列表初始化，且不论是对象，还是基础类型，还是new出来的临时对象（==这个只能用不带=的==），列表初始化的方式都是统一的这两种

列表初始化还能用于函数返回值
```cpp
Person func()
{
    return { 9527, "华安" };
}
```

这里的return { 9527, "华安" }，相当于return（9527，"华安"),**==所以列表初始化十分便捷，它统一了各种变量的初始化方式**


## 列表初始化的细节

见[大丙](https://subingwen.cn/cpp/list-init/#2-%E5%88%97%E8%A1%A8%E5%88%9D%E5%A7%8B%E5%8C%96%E7%BB%86%E8%8A%82)
重点理解：
   1. 聚合体的概念(可以简单理解为，只是一堆基础类型，函数的集合，没有类独特的东西，比如虚函数，构造函数，继承)
   2. 非聚合体实现列表初始化==其实是通过调用合适的自定义构造函数实现的==

# std::initializer_list
**作用：获取任意长度的初始化列表，而非固定长度**

1. 有迭代器，支持遍历，但是遍历时迭代器是**只读**的
2. 对于std::initializer_list而言，可以传入任意长度的初始化列表，但是必须是**同种类型的**
3. 有三个接口，**size(),begin(),end()**
4. std::initializer_list对象**只能被整体初始化或赋值**


**==std::initializer_list效率非常高，本质上它存储的是初始化列表中数据的引用==**
## 作为普通函数参数

**==如果想要函数传入任意个同种类型的参数，可以使用std::initializer_list，只需传参时传入参数列表，或者用{}包住要传入的参数==**


```cpp
void traversal(std::initializer_list<int> a)
{
	for(auto it=a.begin(),it!=a.end(),it++)
	{
		cout<<*it<<' ';
	}
	cout<<endl;
}
std::initializer_list list;
list={1,2,3,4,5,6}
traversal(list);//输出1 2 3 4 5 6
traversal({1,4,5});//输出1 4 5
```


## 作为构造函数参数

当自定义的类的构造函数需要接受任意个参数来初始化时可以使用std::initializer_list,常见例子：需要构造函数初始化数据成员内的vector时

```cpp
#include <iostream>
#include <string>
#include <vector>
using namespace std;

class Test
{
public:
    Test(std::initializer_list<string> list)
    {
        for (auto it = list.begin(); it != list.end(); ++it)
        {
            cout << *it << " ";
            m_names.push_back(*it);
        }
        cout << endl;
    }
private:
    vector<string> m_names;
};

int main(void)
{
    Test t({ "jack", "lucy", "tom" });
    Test t1({ "hello", "world", "nihao", "shijie" });
    return 0;
}
```



# 可调用函数包装器和绑定器

**可调用对象：就是一切可以以函数形式调用的对象
















# 默认函数控制

## =default

在类中有六个编译器会帮我们自动生成的默认函数

1. 无参构造函数：创建类对象
2. 拷贝构造函数：拷贝类对象
3. 移动构造函数：拷贝类对象
4. 拷贝赋值函数：类对象赋值
5. 移动赋值函数：类对象赋值
6. 析构函数 ：销毁类对象

**有时候我们需要去实现这几个函数，但一旦程序员手动实现这几个默认函数，那么这个类就将不是POD类型，我们可能损失POD类型的好处**

解决方法：用=default将**这六个函数**显式声明为显式默认函数

```cpp
class Base
{
//类内定义为默认函数
public:
    Base()=default;
    Base(const Base& obj)=default;
    Base(Base&& obj)=default;
    Base& operator= (const Base& obj)=default;
    Base& operator= (Base&& obj)=default;
    ~Base()=default;
};
// 在类定义之外指定成员函数为默认函数
Base::Base() = default;
Base::Base(const Base& obj) = default;
Base::Base(Base&& obj) = default;
Base& Base::operator= (const Base& obj) = default;
Base& Base::operator= (Base&& obj) = default;
Base::~Base() = default;

```

既可以在类内声明为default也可以在类外声明为default

**==注意：default只能修饰这六个函数，其他函数一律不能用default修饰==**

## =delete

用于修饰一个成员函数**显式删除**，也就是**禁用这个函数**，==这个方法可以有效防止一些预期之外的自动隐式类型转换

```cpp
class Base
{
public:
    Base(int num) : m_num(num) {}
    Base(char c) = delete;
    void print(char c) = delete;
    void print()
    {
        cout << "num: " << m_num << endl;
    }
    void print(int num)
    {
        cout << "num: " << num << endl;
    }
private:
    int m_num;
};

int main()
{
    Base b(97);       // 'a' 对应的 acscii 值为97
    Base b1('a');     // error
    b.print();
    b.print(97);
    b.print('a');     // error
    return 0;
}
```
**这里禁用了参数为char的构造函数，避免了char->int的隐式类型转换** 和explicit同理


# friend

## 语法改进

**==声明一个类为另外一个类的友元时，不再需要使用class关键字，并且还可以使用类的别名（使用 typedef 或者 using 定义）。


## 为类模版声明友元
```cpp
class Tom;

template<typename T>  
class Person
{
    friend T;
};

int main()
{
    Person<Tom> p;
    Person<int> pp;
    return 0;
}
```

不需要class关键字后，**如果实例化时为一个类那么这个类就是Person类的友元，如果实例化时T是一个基础类型，那么friend关键字会被忽略，不会报错，这样就实现了为类模板声明友元**


# 强类型枚举

参见[枚举](obsidian://open?vault=Obsidian%20Vault&file=c%2B%2B%E8%AF%AD%E6%B3%95%E5%8F%8A%E5%BA%93%2F%E6%9E%9A%E4%B8%BE%EF%BC%88enum%EF%BC%89)


# 非受限联合体

联合体内可存放对象类型变多
**==但引用类型一定不能放在联合体中，指针可以==**
联合体的语义要求被管理对象的内存需要受联合体管理，但引用的实际对象在联合体之外


# 处理时间的chrono库

参见[chrono](obsidian://open?vault=Obsidian%20Vault&file=c%2B%2B%E8%AF%AD%E6%B3%95%E5%8F%8A%E5%BA%93%2Fchrono%E5%BA%93%EF%BC%88%E6%97%B6%E9%97%B4%E7%9B%B8%E5%85%B3%E5%BA%93%EF%BC%89)



# 多线程编程

**头文件thread**


## thread
用于创建子线程

### 构造函数
```cpp
// ①
thread() noexcept;
// ②
thread( thread&& other ) noexcept;//可以移动复制，原来的线程放弃资源
// ③
template< class Function, class... Args >
explicit thread( Function&& f, Args&&... args );//传入可调用对象，后面的参数包是可调用对象的参数
// ④
thread( const thread& ) = delete;//拷贝构造函数被禁止，因为线程资源有独占性


```
一个程序运行后，main（）函数的线程为主线程

### get_id
函数原型
```cpp
std::thread::id get_id() const noexcept;
```

父线程可以通过调用子线程的get_id方法得到子线程的id

### std::this_thread::get_id

获取当前线程的id


### join

解决的问题：**==主线程可能在子线程完成任务之前就完成任务销毁，而默认父线程销毁会跟着销毁子线程，所以可能导致任务无法正确完成==**
```cpp
void join();
```

调用join函数的线程会等待该子线程的任务完成后才继续任务，所以join相当于是一个线程阻塞函数
**==注意：阻塞的是调用join函数的当前线程==**
join会自动销毁回收子线程资源
```cpp
int main()
{
    cout << "主线程的线程ID: " << this_thread::get_id() << endl;
    thread t(func, 520, "i love you");
    thread t1(func1);
    cout << "线程t 的线程ID: " << t.get_id() << endl;
    cout << "线程t1的线程ID: " << t1.get_id() << endl;
    t.join();//主线程在这里会等待t线程完成才继续往下
    t1.join();//同理，t1完成后主线程才会销毁
}

```


### detach

线程分离函数，调用该函数的线程和对应的子线程分离，**主线程无法再控制子线程**，包括但不限于join和查看子线程id，**任务完成后，子线程会自动销毁回收资源**
```cpp
void detach();
```
detach不会阻塞线程，容易错误，建议使用join


### joinable

joinable()函数用于判断主线程和子线程是否处于关联（连接）状态，一般情况下，二者之间的关系处于关联状态，该函数返回一个布尔类型：

1. 返回值为true：主线程和子线程之间有关联（连接）关系
2. 返回值为false：主线程和子线程之间没有关联（连接）关系


```cpp
bool joinable() const noexcept;
```


结论：
1. 在创建的子线程对象的时候，**如果没有指定任务函数，那么子线程不会启动，主线程和这个子线程也不会进行连接**
2. 在创建的子线程对象的时候，**如果指定了任务函数，子线程启动并执行任务，主线程和这个子线程自动连接成功**
3. 子线程调用了detach()函数之后，父子线程分离，同时二者的连接断开，调用joinable()返回false
4. 在子线程调用了join()函数，子线程中的任务函数继续执行，直到任务处理完毕，**这时join()会清理（回收）当前子线程的相关资源，所以这个子线程和主线程的连接也就断开了，因此，调用join()之后再调用joinable()会返回false。**


### operator=

```cpp
// move (1)	
thread& operator= (thread&& other) noexcept;
// copy [deleted] (2)	
thread& operator= (const other&) = delete;

```

**因为线程资源不能被复制**

==所以由以上=重载可知，移动赋值可以，但拷贝赋值是不允许的


### 静态函数

thread提供了一个静态函数，可以返回当前设备的CPU核心数，**根据这个数在程序中创建数量相同的线程数，并让每个线程独占一个CPU核心，此时的并行效率最高**

```cpp
static unsigned hardware_concurrency() noexcept;
```
```cpp
int main()
{
	int num=std::thread::hardware_concurrency();
	cout<<"当前设备的CPU核心数"<<num;
}
```



## 命名空间this_thread

c++11,提供了一个可以处理当前线程的命名空间，该命名空间中有四个公共成员函数来控制当前线程，调用时都要加**this_thread::**

### get_id
```cpp
this_thread::id get_id() noexcept;
```

### sleep_for

调用该函数的线程会立刻从**运行态**变为**阻塞态**并等待一段时间,此时不会占用CPU资源

搭配chrono时间库的duration使用
```cpp
template <class Rep, class Period>
  void sleep_for (const chrono::duration<Rep,Period>& rel_time);
```

**注意：线程休眠结束后，线程会变为==就绪态==而非运行态，也就是说休眠结束的线程仍然需要去抢CPU时间片才能重新运行**

### sleep_until

另一个休眠函数，作用和sleep_for一样，但sleep_until是基于时间点来休眠的，休眠到指定时间点
```cpp
template <class Clock, class Duration>
  void sleep_until (const chrono::time_point<Clock,Duration>& abs_time);
```


### yield

yield(屈服),调用该函数后，当前线程会**让出CPU时间片**，并转为**就绪态**，让别的线程有更大概率抢到时间片，**不过需要注意的是：变为就绪态的当前线程，也会立刻进入下一轮时间片抢夺中，所以是有概率抢回来的，这是概率问题**

```cpp
void yield() noexcept;
```

```cpp
#include <iostream>
#include <thread>
using namespace std;

void func()
{
    for (int i = 0; i < 100000000000; ++i)
    {
        cout << "子线程: " << this_thread::get_id() << ", i = " << i << endl;
        this_thread::yield();
    }
}

int main()
{
    thread t(func);
    thread t1(func);
    t.join();
    t1.join();
}
```
在上面的程序中，执行func()中的**for循环会占用大量的时间**，在极端情况下，如果当前线程占用CPU资源不释放就会导致其他线程中的任务无法被处理，或者该线程每次都能抢到CPU时间片，导致其他线程中的任务没有机会被执行。**解决方案就是每执行一次循环，让该线程主动放弃CPU资源，重新和其他线程再次抢夺CPU时间片，如果其他线程抢到了CPU时间片就可以执行相应的任务了。**

结论：

1. **==std::this_thread::yield() 的目的是避免一个线程长时间占用CPU资源，从而导致多线程处理性能下降==**
2. **==std::this_thread::yield() 是让当前线程主动放弃了当前自己抢到的CPU资源，但是在下一轮还会继续抢==**


## std::call_once函数

当某些函数只能在多线程环境下被执行一次时，可以使用call_once函数确保**回调函数**只被运行一次

什么是**回调函数**？：**==函数作为参数传入另一个函数，也就是把你的代码交给另一个人，由另一个人在合适的时机调用==**

比如：你让餐馆店员饭做好了就叫你，那么“做饭”这个函数就拿到了你留下的“叫你”这个**回调函数**
什么时候“叫你”取决于“做饭”这个函数的进度决定


```cpp
// 定义于头文件 <mutex>
template< class Callable, class... Args >
void call_once( std::once_flag& flag, Callable&& f, Args&&... args );//flag是一个once_flag类对象，直接once_flag fl;定义就行，f是可调用对象（那个只能被运行一次的函数），后面是那个函数的参数
```

```cpp
#include <iostream>
#include <thread>
#include <mutex>
using namespace std;

once_flag g_flag;
void do_once(int a, string b)
{
    cout << "name: " << b << ", age: " << a << endl;
}

void do_something(int age, string name)
{
    static int num = 1;
    call_once(g_flag, do_once, 19, "luffy");
    cout << "do_something() function num = " << num++ << endl;
}

int main()
{
    thread t1(do_something, 20, "ace");
    thread t2(do_something, 20, "sabo");
    thread t3(do_something, 19, "luffy");
    t1.join();
    t2.join();
    t3.join();

    return 0;
}
```
输出
```cpp
name: luffy, age: 19
do_something() function num = 1
do_something() function num = 2
do_something() function num = 3
```
可以看到，虽然有三个线程调用了dosomething函数，但是do_once函数只被调用了一次

## 线程同步之互斥锁

当多个线程对同一个数据同时写或同时读写时，数据容易出现错误，现在就必须要**线程同步**来保证**线程的执行顺序是有序的**

==临界区：有关全局共享资源的上下文代码==
### std::mutex

#### 成员函数

```cpp
void lock();
```
lock函数的作用是给临界区上锁，如果当前临界区是打开的，**调用lock函数的线程会互斥锁的所有权，并且给临界区上锁**，其他线程会被阻塞在lock外，**等待抢锁**，当线程用完之后，**临界区解锁，其他被阻塞的线程抢锁，抢到锁则继续处理，抢不到则继续阻塞**

```cpp
bool try_lock();
```
与lock函数作用基本相似，**==但try_lock不会阻塞线程==**
1. 如果互斥锁是未锁定状态，得到了互斥锁所有权并加锁成功，函数返回true
2. 如果互斥锁是锁定状态，无法得到互斥锁所有权加锁失败，函数返回false
**仅仅只是试一下获取互斥锁所有权并上锁，如果失败不会阻塞在临界区上方**


```cpp
void unlock();
```
解锁函数，**==只有给这个临界区上锁的线程有权限解锁==**

**互斥锁线程同步的步骤**
1. 找到多个线程操作的共享资源（全局变量、堆内存、类成员变量等），也可以称之为临界资源
2. 找到和共享资源有关的上下文代码，也就是临界区
3. 在临界区的上边调用互斥锁类的lock()方法
4. 在临界区的下边调用互斥锁的unlock()方法


**==一定要在临界区下方调用unlock解锁，否则所有的别的线程都会被一直阻塞==**



1. ==在所有线程的任务函数执行完毕之前，**互斥锁对象是不能被析构的，一定要在程序中保证这个对象的可用性**。
2. ==**互斥锁的个数和共享资源的个数相等**，也就是说每一个共享资源都应该对应一个互斥锁对象。互斥锁对象的个数和线程的个数没有关系。（**一个共享对象要一个互斥锁来保护它的上下文临界区**）== 这样锁粒度高，并发性高，性能好，但管理难

### std::lock_guard

**==lock_guard是一个模板，叫做哨兵锁，但它不是一种锁类型！！！！，它负责管理各种各样的锁安全解锁==**

它可以简化lock，unlock的写法，并且用了**RAII**技术，当被保护元素销毁时，互斥锁也自动销毁，避免忘记写unlock导致的死锁


```cpp
// 类的定义，定义于头文件 <mutex>
template< class Mutex >
class lock_guard;

// 常用构造函数
explicit lock_guard( mutex_type& m );

```
使用
```cpp
std::lock_guard<mutex> lock(mutex_value)//mutex_value，构造时自动锁定互斥锁，不需要手动维护
```


**==但是这种方式也有弊端，在上面的示例程序中整个for循环的体都被当做了临界区，多个线程是线性的执行临界区代码的，因此临界区越大程序效率越低==**


### std::recursive_mutex

如果同一个线程重复获取一个互斥锁，会导致死锁，会导致程序异常退出，因为锁已经被该线程第一次获取互斥锁时锁住了，原则上，它自己也无法获取这个被锁住的锁，所以导致死锁阻塞

递归获取锁导致死锁例子：
```cpp
#include <iostream>
#include <thread>
#include <mutex>
using namespace std;

struct Calculate
{
    Calculate() : m_i(6) {}

    void mul(int x)
    {
        lock_guard<recursive_mutex> locker(m_mutex);
        m_i *= x;
    }

    void div(int x)
    {
        lock_guard<recursive_mutex> locker(m_mutex);
        m_i /= x;
    }

    void both(int x, int y)
    {
        lock_guard<recursive_mutex> locker(m_mutex);
        mul(x);
        div(y);
    }

    int m_i;
    recursive_mutex m_mutex;
};

int main()
{
    Calculate cal;
    cal.both(6, 3);
    cout << "cal.m_i = " << cal.m_i << endl;
    return 0;
}
```





为了解决这一问题，可以使用**std::recursive_mutex类型**，它允许同一线程多次获取一个互斥锁

**虽然递归互斥锁可以解决同一个互斥锁频繁获取互斥锁资源的问题，但是还是建议少用，主要原因如下：**
1. 使用递归互斥锁的场景往往都是可以简化的，使用递归互斥锁很容易放纵复杂逻辑的产生，从而导致bug的产生
2. 递归互斥锁比非递归互斥锁效率要低一些。
3. 递归互斥锁虽然允许同一个线程多次获得同一个互斥锁的所有权，但最大次数并未具体说明，一旦超过一定的次数，就会抛出std::system错误。



### std::timed_mutex

std::timed_mutex是超时独占互斥锁，主要是在获取互斥锁资源时增加了**超时等待功能**，因为不知道获取锁资源需要等待多长时间，**为了保证不一直等待下去，设置了一个超时时长，超时后线程就可以解除阻塞去做其他事情了。**

#### 成员函数
```cpp
void lock();
bool try_lock();
void unlock();//与mutex的成员函数相同

// std::timed_mutex比std::_mutex多出的两个成员函数
template <class Rep, class Period>
  bool try_lock_for (const chrono::duration<Rep,Period>& rel_time);//等待一个duration

template <class Clock, class Duration>
  bool try_lock_until (const chrono::time_point<Clock,Duration>& abs_time);//等待到一个时间点
```

递归超时互斥锁std::recursive_timed_mutex的使用方式和std::timed_mutex是一样的，只不过它可以允许一个线程多次获得互斥锁所有权，而std::timed_mutex只允许线程获取一次互斥锁所有权。另外，递归超时互斥锁std::recursive_timed_mutex也拥有和std::recursive_mutex一样的弊端，不建议频繁使用。


## 条件变量

 条件变量是C++11提供的另外一种用于等待的同步机制，它能阻塞一个或多个线程，直到收到另外一个线程发出的通知或者超时时，才会唤醒当前阻塞的线程。条件变量需要和互斥量配合起来使用，C++11提供了两种条件变量：

**==核心作用：如果没有条件变量，线程会一直循环检查条件（比如队列是否为空），而条件变量让条件不满足时直接让线程休眠，直到线程被别的线程唤醒==**

**condition_variable**：需要配合std::unique_lock<std::mutex>进行wait操作，也就是阻塞线程的操作。
**condition_variable_any**：可以和任意带有lock()、unlock()语义的mutex搭配使用，也就是说有四种：
1. std::mutex：独占的非递归互斥锁
2. std::timed_mutex：带超时的独占非递归互斥锁
3. std::recursive_mutex：不带超时功能的递归互斥锁
4. std::recursive_timed_mutex：带超时的递归互斥锁
条件变量通常用于**生产者和消费者模型**，大致使用过程如下：

```cpp
拥有条件变量的线程获取互斥量
循环检查某个条件，如果条件不满足阻塞当前线程，否则线程继续向下执行
产品的数量达到上限，生产者阻塞，否则生产者一直生产。。。
产品的数量为零，消费者阻塞，否则消费者一直消费。。。
条件满足之后，可以调用notify_one()或者notify_all()唤醒一个或者所有被阻塞的线程
由消费者唤醒被阻塞的生产者，生产者解除阻塞继续生产。。。
由生产者唤醒被阻塞的消费者，消费者解除阻塞继续消费。。。
```



### condition_variable
 
1.1 成员函数
condition_variable的成员函数主要分为两部分：线程等待（阻塞）函数 和线程通知（唤醒）函数，这些函数被定义于头文件 <condition_variable>。

等待函数

调用wait()函数的线程会被阻塞
```cpp
// ①
void wait (unique_lock<mutex>& lck);
// ②
template <class Predicate>
void wait (unique_lock<mutex>& lck, Predicate pred);
```
函数①：调用该函数的线程直接被阻塞

函数②：该函数的第二个参数是一个判断条件，是一个返回值为布尔类型的函数

**该参数可以传递一个有名函数的地址，也可以直接指定一个匿名函数**

**==表达式返回false当前线程被阻塞，表达式返回true当前线程不会被阻塞，继续向下执行==**


==独占的互斥锁对象不能直接传递给wait()函数，需要通过模板类**unique_lock**进行二次处理，通过得到的对象仍然可以对独占的互斥锁对象做如下操作，使用起来更灵活。==

1. lock	锁定关联的互斥锁
2. try_lock	尝试锁定关联的互斥锁，若无法锁定，函数直接返回
3. try_lock_for	试图锁定关联的可定时锁定互斥锁，若互斥锁在给定时长中仍不能被锁定，函数返回
4. try_lock_until	试图锁定关联的可定时锁定互斥锁，若互斥锁在给定的时间点后仍不能被锁定，函数返回
5. unlock	将互斥锁解锁

如果线程被该函数阻塞，**这个线程会释放占有的互斥锁的所有权，当阻塞解除之后这个线程会重新得到互斥锁的所有权**，继续向下执行（这个过程是在函数内部完成的，了解这个过程即可，其目的是为了避免线程的死锁）。


**wait_for()**函数和wait()的功能是一样的，只不过多了一个阻塞时长，假设阻塞的线程没有被其他线程唤醒，当阻塞时长用完之后，线程就会自动解除阻塞，继续向下执行。
```cpp
template <class Rep, class Period>
cv_status wait_for (unique_lock<mutex>& lck,
                    const chrono::duration<Rep,Period>& rel_time);
	
template <class Rep, class Period, class Predicate>
bool wait_for(unique_lock<mutex>& lck,
               const chrono::duration<Rep,Period>& rel_time, Predicate pred);
```

**wait_until()**函数和wait_for()的功能是一样的，它是指定让线程阻塞到某一个时间点，假设阻塞的线程没有被其他线程唤醒，当到达指定的时间点之后，线程就会自动解除阻塞，继续向下执行。
```cpp
template <class Clock, class Duration>
cv_status wait_until (unique_lock<mutex>& lck,
                      const chrono::time_point<Clock,Duration>& abs_time);

template <class Clock, class Duration, class Predicate>
bool wait_until (unique_lock<mutex>& lck,
                 const chrono::time_point<Clock,Duration>& abs_time, Predicate pred);
```

#### 通知函数
```cpp
void notify_one() noexcept;
void notify_all() noexcept;
```

notify_one()：唤醒一个被当前条件变量阻塞的线程
notify_all()：唤醒全部被当前条件变量阻塞的线程
### 生产者和消费者模型
我们可以使用条件变量来实现一个同步队列，这个队列作为生产者线程和消费者线程的共享资源，示例代码如下：
```cpp
#include <iostream>
#include <thread>
#include <mutex>
#include <list>
#include <functional>
#include <condition_variable>
using namespace std;

class SyncQueue
{
public:
    SyncQueue(int maxSize) : m_maxSize(maxSize) {}
    
    void put(const int& x)
    {
        unique_lock<mutex> locker(m_mutex);
        // 判断任务队列是不是已经满了
        while (m_queue.size() == m_maxSize)
        {
            cout << "任务队列已满, 请耐心等待..." << endl;
            // 阻塞线程
            m_notFull.wait(locker);
        }
        // 将任务放入到任务队列中
        m_queue.push_back(x);
        cout << x << " 被生产" << endl; 
        // 通知消费者去消费
        m_notEmpty.notify_one();
    }

    int take()
    {
        unique_lock<mutex> locker(m_mutex);
        while (m_queue.empty())
        {
            cout << "任务队列已空，请耐心等待。。。" << endl;
            m_notEmpty.wait(locker);
        }
        // 从任务队列中取出任务(消费)
        int x = m_queue.front();
        m_queue.pop_front();
        // 通知生产者去生产
        m_notFull.notify_one();
        cout << x << " 被消费" << endl;
        return x;
    }

    bool empty()
    {
        lock_guard<mutex> locker(m_mutex);
        return m_queue.empty();
    }

    bool full()
    {
        lock_guard<mutex> locker(m_mutex);
        return m_queue.size() == m_maxSize;
    }

    int size()
    {
        lock_guard<mutex> locker(m_mutex);
        return m_queue.size();
    }

private:
    list<int> m_queue;     // 存储队列数据
    mutex m_mutex;         // 互斥锁
    condition_variable m_notEmpty;   // 不为空的条件变量
    condition_variable m_notFull;    // 没有满的条件变量
    int m_maxSize;         // 任务队列的最大任务个数
};

int main()
{
    SyncQueue taskQ(50);
    auto produce = bind(&SyncQueue::put, &taskQ, placeholders::_1);
    auto consume = bind(&SyncQueue::take, &taskQ);
    thread t1[3];
    thread t2[3];
    for (int i = 0; i < 3; ++i)
    {
        t1[i] = thread(produce, i+100);
        t2[i] = thread(consume);
    }

    for (int i = 0; i < 3; ++i)
    {
        t1[i].join();
        t2[i].join();
    }

    return 0;
}
```


**用wait函数的重载重写**上面示例程序中的put()、take()函数可以做如下修改：
```cpp
void put(const int& x)
{
    unique_lock<mutex> locker(m_mutex);
    // 根据条件阻塞线程
    m_notFull.wait(locker, [this]() {
        return m_queue.size() != m_maxSize;
    });
    // 将任务放入到任务队列中
    m_queue.push_back(x);
    cout << x << " 被生产" << endl;
    // 通知消费者去消费
    m_notEmpty.notify_one();
}


int take()
{
    unique_lock<mutex> locker(m_mutex);
    m_notEmpty.wait(locker, [this]() {
        return !m_queue.empty();
    });
    // 从任务队列中取出任务(消费)
    int x = m_queue.front();
    m_queue.pop_front();
    // 通知生产者去生产
    m_notFull.notify_one();
    cout << x << " 被消费" << endl;
    return x;
}
```

**推荐使用这种直接接入Lambda函数作判断函数，这样可以解决虚假唤醒问题，避免使用外接判断if语句来wait。因为if只判断一次，状态可能仍为false就唤醒线程了（虚假唤醒），可以用while循环一直检查，这种形式就是内部wait自动while循环检查的**

修改之后可以发现，程序变得更加精简了，而且执行效率更高了，因为在这两个函数中的while循环被删掉了，但是最终的效果是一样的，**==推荐使用这种方式的wait()进行线程的阻塞。==**

### condition_variable_any

**成员函数**
condition_variable_any的成员函数也是分为两部分：线程等待（阻塞）函数 和线程通知（唤醒）函数，这些函数被定义于头文件 <condition_variable>。

等待函数

**==condition_variable_any 的wait函数直接传互斥锁就行了==**


```cpp
// ①
template <class Lock> void wait (Lock& lck);
// ②
template <class Lock, class Predicate>
void wait (Lock& lck, Predicate pred);
```
函数①：调用该函数的线程直接被阻塞
函数②：该函数的第二个参数是一个判断条件，是一个返回值为布尔类型的函数
1. 该参数可以传递一个有名函数的地址，也可以直接指定一个匿名函数
2. 表达式返回false当前线程被阻塞，表达式返回true当前线程不会被阻塞，继续向下执行


**==可以直接传递给wait()函数的互斥锁类型有四种，分别是：
std::mutex、std::timed_mutex、std::recursive_mutex、std::recursive_timed_mutex==**

**其他成员函数和condition_variable一样**


#### 生产者和消费者模型
使用条件变量**condition_variable_any**同样可以实现上面的生产者和消费者的例子，代码只有个别细节上有所不同：
```cpp
#include <iostream>
#include <thread>
#include <mutex>
#include <list>
#include <functional>
#include <condition_variable>
using namespace std;

class SyncQueue
{
public:
    SyncQueue(int maxSize) : m_maxSize(maxSize) {}

    void put(const int& x)
    {
        lock_guard<mutex> locker(m_mutex);//这个代码可能有问题，因为lock_guard不支持临时释放锁
        // 根据条件阻塞线程
        m_notFull.wait(m_mutex, [this]() {
            return m_queue.size() != m_maxSize;
        });
        // 将任务放入到任务队列中
        m_queue.push_back(x);
        cout << x << " 被生产" << endl;
        // 通知消费者去消费
        m_notEmpty.notify_one();
    }

    int take()
    {
        lock_guard<mutex> locker(m_mutex);
        m_notEmpty.wait(m_mutex, [this]() {
            return !m_queue.empty();
        });
        // 从任务队列中取出任务(消费)
        int x = m_queue.front();
        m_queue.pop_front();
        // 通知生产者去生产
        m_notFull.notify_one();
        cout << x << " 被消费" << endl;
        return x;
    }

    bool empty()
    {
        lock_guard<mutex> locker(m_mutex);
        return m_queue.empty();
    }

    bool full()
    {
        lock_guard<mutex> locker(m_mutex);
        return m_queue.size() == m_maxSize;
    }

    int size()
    {
        lock_guard<mutex> locker(m_mutex);
        return m_queue.size();
    }

private:
    list<int> m_queue;     // 存储队列数据
    mutex m_mutex;         // 互斥锁
    condition_variable_any m_notEmpty;   // 不为空的条件变量
    condition_variable_any m_notFull;    // 没有满的条件变量
    int m_maxSize;         // 任务队列的最大任务个数
};

int main()
{
    SyncQueue taskQ(50);
    auto produce = bind(&SyncQueue::put, &taskQ, placeholders::_1);
    auto consume = bind(&SyncQueue::take, &taskQ);
    thread t1[3];
    thread t2[3];
    for (int i = 0; i < 3; ++i)
    {
        t1[i] = thread(produce, i + 100);
        t2[i] = thread(consume);
    }

    for (int i = 0; i < 3; ++i)
    {
        t1[i].join();
        t2[i].join();
    }

    return 0;
}
```


**总结**： 
condition_variable 配合 unique_lock 使用更灵活一些，可以在在任何时候自由地释放互斥锁，而condition_variable_any 如果和lock_guard 一起使用必须要等到其生命周期结束才能将互斥锁释放。但是，condition_variable_any 可以和多种互斥锁配合使用，应用场景也更广，而 condition_variable 只能和独占的非递归互斥锁（mutex）配合使用，有一定的局限性。



# 原子变量

c++11提供了一个原子类型 std::atomic\<T>,这个原子类型内部管理的变量就被称为原子变量。

**注意：原子类型中的T只能指定为整型，指针等类型（不支持浮点型和复合类型）

原子指的是一系列不可被CPU上下文交换的机器指令，这些指令组合在一起就形成了**原子操作**。在多核CPU下，当某个CPU核心开始运行原子操作时，会先暂停其它CPU内核对内存的操作，以保证原子操作不会被其它CPU内核所干扰。

由于原子操作是通过指令提供的支持，因此它的性能相比锁和消息传递会好很多。相比较于锁而言，原子类型不需要开发者处理加锁和释放锁的问题，同时支持修改，读取等操作，还具备较高的并发性能，几乎所有的语言都支持原子类型。

**可以看出原子类型是无锁类型，但是无锁不代表无需等待，因为原子类型内部使用了CAS循环（当第一次修改失败，会立即重试，这样循环下去，宏观上形成等待的感觉），当大量的冲突发生时，该等待还是得等待！但是总归==比锁要好。==**

``CAS全称是Compare and swap, 它通过一条指令读取指定的内存地址，然后判断其中的值是否等于给定的前置值，如果相等，则将其修改为新的值,如果不是，说明变量被人修改过，那就重试


## 成员函数

构造函数略，它**不支持拷贝构造和在类外部复制对象，但是可以在内部赋值（=）T**


原子地以 desired 替换当前值。按照 order 的值影响内存。
```cpp
void store( T desired, std::memory_order order = std::memory_order_seq_cst ) noexcept;
void store( T desired, std::memory_order order = std::memory_order_seq_cst ) volatile noexcept;
```

desired：存储到原子变量中的值
order：强制的**内存顺序**

原子地加载并返回原子变量的当前值。按照 order 的值影响内存。直接访问原子对象也可以得到原子变量的当前值。
```cpp
T load( std::memory_order order = std::memory_order_seq_cst ) const noexcept;
T load( std::memory_order order = std::memory_order_seq_cst ) const volatile noexcept;

```



### 内存顺序约束
通过上面的 API 函数我们可以看出，在调用 atomic类提供的 API 函数的时候，需要指定原子顺序，在C++11给我们提供的 API 中使用枚举用作执行原子操作的函数的实参，以指定如何同步不同线程上的其他操作。

定义如下:
```cpp
typedef enum memory_order {
    memory_order_relaxed,   // relaxed
    memory_order_consume,   // consume
    memory_order_acquire,   // acquire
    memory_order_release,   // release
    memory_order_acq_rel,   // acquire/release
    memory_order_seq_cst    // sequentially consistent
} memory_order;
```

1. memory_order_relaxed， 这是最宽松的规则，它对编译器和CPU不做任何限制，可以乱序
2. memory_order_release 释放，设定内存屏障(Memory barrier)，保证它之前的操作永远在它之前，但是它后面的操作可能被重排到它前面
3. memory_order_acquire 获取, 设定内存屏障，保证在它之后的访问永远在它之后，但是它之前的操作却有可能被重排到它后面，往往和Release在不同线程中联合使用
4. memory_order_consume：改进版的memory_order_acquire ，开销更小
5. memory_order_acq_rel，它是Acquire 和 Release 的结合，同时拥有它们俩提供的保证。比如你要对一个 atomic 自增 1，同时希望该操作之前和之后的读取或写入操作不会被重新排序
6. memory_order_seq_cst 顺序一致性， memory_order_seq_cst 就像是memory_order_acq_rel的加强版，它不管原子操作是属于读取还是写入的操作，只要某个线程有用到memory_order_seq_cst 的原子操作，线程中该memory_order_seq_cst 操作前的数据操作绝对不会被重新排在该memory_order_seq_cst 操作之后，且该memory_order_seq_cst 操作后的数据操作也绝对不会被重新排在memory_order_seq_cst 操作前。



**==如何使用？：只需要把共享变量声明为原子变量，这样子对它的读写就能类似加锁一样线程安全了==**







