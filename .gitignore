# Ignore everything by default.
*

# Always keep the .gitignore file itself.
!.gitignore

# Keep directories so files inside them can be re-included.
!*/

# Keep all Markdown notes.
!*.md

# Keep the photo folder and every image inside it.
!photo/
!photo/**
